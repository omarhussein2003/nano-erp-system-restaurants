# Application Settings
DEBUG=true
SECRET_KEY=your-secret-key-here

# Database Configuration
# The system uses SQLite by default (erp.db file)
# No additional database configuration needed for standalone operation
